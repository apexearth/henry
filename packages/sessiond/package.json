{
  "name": "@henry/sessiond",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Henry's PTY owner: a long-lived Node process that keeps Claude sessions alive across daemon restarts.",
  "scripts": {
    "start": "node src/main.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "node-pty": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
