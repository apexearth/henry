{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The shell owns no IPC surface; it only drives the window from Rust.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
