[package]
name = "henry-shell"
version = "0.1.0"
edition = "2021"
rust-version = "1.77"
description = "Native window for Henry's UI"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
opt-level = "s"
lto = true
strip = true
