# 2026-09-21

- A peek edits when asked: `edit` in the header swaps the source for CodeMirror (undo, multiple cursors, find and replace, folds, Tab indents; grammars on demand, coloured by the read view's own classes so the light shade comes free), ⌘S saves through `POST /api/file` carrying the mtime it read, so a file a session changed meanwhile is refused with `save anyway` rather than overwritten; a markdown split shows the page of what you are typing
- The files tree makes files: right-click a root, a folder or a file → `new file in …`, a name row under that folder (slashes make folders), and the empty file opens straight in the editor
- Ctrl+C copies a terminal selection over the LAN too: reached by IP rather than localhost the page is an insecure origin with no clipboard API, so the key was clearing the selection and copying nothing; it now falls back to the legacy copy command, which xterm fills from its selection
