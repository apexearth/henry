# 2026-09-24

- Henry starts from the Start menu as one window: `scripts/henry.vbs` runs `bun run dev --app` with no console, closing the window stops the daemon and Vite (their whole process tree), and a second launch while they are up just opens another window
