# 2026-09-15

- Henry's documents are split by what they are for, because a thousand-line plan is one nobody reads in a pass: `PLAN.md` keeps only what is ahead, `docs/` says how each area works, `changelog/` says what shipped by day
- The file tree's marks are git status, because a commit has to clear them and every session in a repo should see the same wall
- Rules can differ per repo, because a flag that fires on every off-chain session teaches you to ignore the badge
- PLAN.md says the decision in two lines, because it is a contract and not the place the reasoning lives
- Henry's voice sees every machine and can open a session for you, because a roster that names a peer's sessions and knows nothing about them is not an answer, and "start one in henry and have it fix the tests" is a thing you say rather than click
- Your hours stop counting Claude's own prompts, because a task reporting in at 3 a.m. is not you at the keyboard
