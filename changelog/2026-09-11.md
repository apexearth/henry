# 2026-09-11

- Every machine shows its own 5h/7d windows, because one pair of bars was one machine answering for the whole rail
- The shell sends new-window links to the default browser, because a webview with no answer drops the click
- A folder wears the changes inside it, because a closed tree should still show where the work is
- Revert "Pi harness: a first sketch of the extension and the daemon's event mapping (draft, unwired)"
- Pi harness: a first sketch of the extension and the daemon's event mapping (draft, unwired)
- Files is the repo view, because a repo's card and its tree were two panes about one thing
- Usage is a status strip along the bottom, because a gauge is glanced at, not visited
- The new-session picker puts exact name matches on top, because the resting order buried them
- The topbar has no files button, because ⌘F already opens the pane
