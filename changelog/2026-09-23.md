# 2026-09-23

- The phone's mic asks again whether Henry can hear on every reconnect, because on iPhone Chrome whisper is the only ear and one status call lost to a daemon restart hid the 🎙 until a reload
- An `.html` peek runs the page, with its relative CSS, scripts and pictures loaded from disk as file:// would load them (`/raw/<path>`), in a sandboxed iframe that never runs as Henry; `page | split | source` and a reload in the header. The daemon now refuses anything but `/raw` from `Origin: null`
- A PDF peek (by its `%PDF-` bytes) is drawn by pdf.js inside a sandboxed, opaque-origin frame, zoomable from a header slider or ⌘+wheel; it waits for a "view" click unless Settings → files says to render PDFs as they open (per browser, off by default).
- ⧉ in a peek's header pops it out into its own window, a linked Tauri window in the shell and a browser popup elsewhere; closing the window closes the peek.
