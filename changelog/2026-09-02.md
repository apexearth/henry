# 2026-09-02

- Neutral default repo (~/code) in config defaults, PLAN and test fixtures
- Dockable layout, plain terminal sessions, single-line rail with terminal titles
- CLAUDE.md: orientation for agents
- henry-sessiond: sessions survive daemon restarts (loopback TCP + token, node-pty only); host field
- Resume exited Claude sessions from the rail; document restart and overseer cadence
- Keep recent sessions across restarts; rate-limit overseer Stop runs
- Henry milestones 2-5: hooks, usage, git, flags, overseer; launch settings; env isolation
- git.ts: noteSessionPath seam for hooks
- Henry milestone 1: terminal host

The plan's five milestones — terminal host; events + usage; git; flags; overseer — all met
today: `bun run dev` brings up the daemon and UI, a real `claude` session runs inside it, and
each panel shows live data from that session (verified in Chrome against a Fable 5.1 session).
