# 2026-09-04

- README: what it does and why, before how it works
- A ⌘-click on a path peeks; the press no longer reaches the PTY too
- MCP port: the daemon keeps answering where its live sessions expect it
- Repos restore: a rehydrated session keeps its own checkout, and boots past a bad one
- Hook port: the daemon publishes it, so a session stops trusting its environment
- Henry's mark: a check and a hat band above 48px, the axe retired
- Asks: a session can call for the user when the work is timed
- MCP through --mcp-config, and a rail delimiter that folds
- Topbar: my hours, prompts and cadence, counted in whole minutes
- Error boundary: a render throw shows the error instead of blanking the window
- Open PRs: a gh-backed count on every repo card and in the topbar
- Safeguards: cap hook payloads, back off slow repos, chunk the transcript read
- Settings page: repos root, history retention, playbook off by default
- Explorer: Tab flips to file search (git grep); ⌘F over a peek finds in that file
- Repos: tree button shows the commit graph, hashes open the commit
- Explorer: ⌘F browses repos and files with a preview, so Zed can stay closed
- Remotes: address editor on its own line so the input has room
- Federation: re-point a paired machine's address without pairing again
- Dev flow runs the native shell too, with a debounced supervisor
- Merge origin/main
- Move to ports 14711/14712/14713
- Windows: sessiond via Start-Process, ConPTY SO/SI as spaces, settling worktrees
- Federation: listener proves the pairing code too; peers cannot reach other peers through us
