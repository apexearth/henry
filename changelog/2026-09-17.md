# 2026-09-17

- A markdown peek is a page, with its mermaid fences drawn, because a README read as fenced source is a README you open somewhere else; `page | split | source` in the header shows it rendered, as lines, or both side by side
- The voice tests typecheck again: a noise generator declared without the sample-time argument every other tone takes
