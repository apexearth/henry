# 2026-09-08

- ⌘` opens a terminal in the folder you are already looking at
- The terminal has no scrollbar, because nobody ever aimed at it
- A drag scrolls the terminal on a phone, because xterm's own touch scroll never ran
- Merge branch 'worktree-agent-a1d2d4e92ea05b9a1'
- Windows sessions carry their ConPTY build, and a window sees usage for every session it hosts
- Henry needs Bun 1.4.2: 1.2.19 kept a kilobyte per hook and statusline request on Windows
- The daemon reports what it holds, and windows stop drowning in usage and repo updates
- Carry over the main checkout's uncommitted work (ConPTY dll, windowsBuild on sessions, merged usage for windows)
