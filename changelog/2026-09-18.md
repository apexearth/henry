# 2026-09-18

- Henry's answers follow the speakers again after a dock is unplugged: the Voice panel's AudioContext is thrown away whenever the audio device set changes, instead of rendering for the rest of its life into an output that is gone
- Voice holds a conversation: the last ten exchanges ride along with each question, so a follow-up has something to follow; dictation stays out of it, and thirty minutes of silence starts fresh
- Henry has a light side, because a phone outdoors is a black mirror: two light shades (`light`, `white`) flip the palette, xterm pulls Claude Code's dark-terminal colours up to a 7:1 floor on paper and its half-opacity dim text is redrawn solid, and the phone's ⋮ sheet grows a ⚙ tab so the switch is where the phone is
- Dim text no longer cuts holes in the context wall: xterm paints every dim run a slab of the theme background, which read as bars on a light shade
